apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: llm-logs

resources:
  - base.yaml
  - data.yaml
  - app.yaml
  # Requires KEDA for the worker ScaledObject. Comment out if not installed.
  - autoscale.yaml

commonLabels:
  app.kubernetes.io/part-of: llm-inference-logging
