apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: llm-logs

resources:
  - base.yaml
  - data.yaml
  - app.yaml
  # Plain CPU HPA; requires metrics-server. Comment out if not installed.
  - autoscale.yaml

commonLabels:
  app.kubernetes.io/part-of: llm-inference-logging
